# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Local developer/session notes — not for distribution (may contain personal
# paths, machine-specific commands, or contact details). Keep out of the repo.
HANDOFF.md

# Scratch/run/verification helpers (underscore-prefixed) — local only, may embed
# machine-specific paths or an email; NEVER commit (use `git add <file>`, not `-A`).
scripts/_*

# Pipeline outputs & working data (generated; don't commit)
*_discovery/
*_discovery/
PACKAGE/
run1/
run2/
run3/
runA/
runB/
runC/
downstream/
*.log
pipeline.log

# Streamed/cached databases & large indexes (regenerated on run)
_shared/
db_setup/
cache/
*.h3m
*.h3i
*.h3f
*.h3p
*.tar.gz
*.fasta.gz
*.fna.gz
*.faa.gz

# Conda env (created by setup.sh, not committed)
envs/
