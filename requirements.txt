# HMM Homologue Finder — external tools and Python dependencies.
# Install everything with:  bash setup.sh   (creates the hmm-discovery conda env)
#
# External command-line tools (via conda-forge / bioconda):
#   hmmer        3.4      (hmmbuild, hmmsearch, hmmscan)
#   mafft        7.526
#   trimal       1.5
#   prodigal     2.6.3
#   seqkit       2.13.0
#   cd-hit       4.8.1
#   iqtree       3.1.2
#   meme         5.5.9    (meme, fimo)
#   clinker      0.0.32   (pip)
#   curl         (system)
#
# Python packages:
biopython>=1.80
pandas>=1.5
